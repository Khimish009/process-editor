import clsx from "clsx";
import { Position } from "../../domain/position";
import styles from "./styles.module.css"

export const Layout = ({ 
	start,
  end,
}: {
  start: Position
  end: Position
}) => {  
    return (
      <div></div>
    )
}
